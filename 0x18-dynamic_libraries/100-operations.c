/**
 * add - Addition
 * @a: Number
 * @b: Nuber
 * Return: 0
 */
int add(int a, int b)
{
      return (a + b);
}
/**
 * sub - addition
 * @a: number
 * @b: nuber
 * Return: 0
 */
int sub(int a, int b)
{
      return (a - b);
}
/**
 * mul - addition
 * @a: number
 * @b: nuber
 * Return: 0
 */
int mul(int a, int b)
{
      return (a * b);
}
/**
 * div - addition
 * @a: number
 * @b: nuber
 * Return: 0
 */
int div(int a, int b)
{
      return (a / b);
}
/**
 * mod - addition
 * @a: number
 * @b: nuber
 * Return: 0
 */
int mod(int a, int b)
{
      return (a % b);
}
