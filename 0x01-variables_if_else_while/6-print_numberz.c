#include <stdio.h>

/**
 * main - this program is about printing a lower case of an alphabet
 *
 * Return: zero (0)
 */
int main(void)

{
	int i = '1';

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar(10);
	return (0);
}
