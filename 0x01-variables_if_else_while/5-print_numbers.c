#include <stdio.h>

/**
 * main - this program is about printing a lower case of an alphabet
 *
 * Return: zero (0)
 */
int main(void)

{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar(10);
	return (0);
}
