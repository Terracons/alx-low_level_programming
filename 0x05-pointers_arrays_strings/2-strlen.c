#include "main.h"

/**
 * _strlen - return
 * the lenth of a strings
 *
 * @s: charter s
 * Return: progarm return n
 */
int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)
	{
		continue;
	}
	return (n);
}
